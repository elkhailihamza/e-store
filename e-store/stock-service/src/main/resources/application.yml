spring:
  application:
    name: stock-service
  config:
    import: optional:configserver:http://localhost:8888

  # Once done, put datasource and jpa in config server as 'stock-service.yml' inside src/resources/configurations
  datasource:
    url: jdbc:postgresql://localhost:5432/stockDB
    password: ;(.314Luiv./
    username: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update